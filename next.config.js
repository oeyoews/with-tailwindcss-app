/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  // distDir: "dist",
  images: {
    domains: ["images.unsplash.com", "www.gravatar.com"],
  },
};

module.exports = nextConfig;
